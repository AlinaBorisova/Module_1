'use strict';

/*const min = 0;
const max = 100;
const num = Math.round(Math.random() * (max - min));
        console.log(num);

const game = () => {
    while (true) {
    const userNum = +prompt('Введите число');
        if (isNaN(userNum)) {
            alert('Нужно ввести число');
            continue;
        };

        if (userNum === null) {
            alert('Пока');
            break;
        };
            
        if (userNum < num) {
            alert('Больше');
            continue;
        } else if (userNum > num) {
            alert('Меньше');
            continue;
        } else if (userNum === num) {
            alert('Правильно');
            break;
        };
    };
};
game();*/